namespace MonopolyProtocol
{
    public enum BroadcastType
    {
        Alert,
        Online,
        LobbyStatusChange,
        GameRoomStatusChange,
        GameInvite,
        SendMessage,
        MonopolyGameEvents
    }
}
