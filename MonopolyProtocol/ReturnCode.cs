namespace MonopolyProtocol
{
    public enum ReturnCode : byte
    {
        Correct,
        NotExist,
        InvalidOperation,
        InvalidParameter,
        PermissionDeny,
        Canceled
    }
}
